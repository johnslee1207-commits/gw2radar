# Evidence Model

Evidence records track source, source URL, fetch time, raw hash, raw payload, and confidence.

MVP 0.1 uses a single mock fixture evidence record:

```text
mock:evidence:mvp_0_1
```

Future real API ingestion can attach API response hashes and source URLs without changing inference rules.
